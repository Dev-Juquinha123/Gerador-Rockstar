#include <iostream>
#include <string>


struct Livro
{
    unsigned paginas;   // Número total de páginas do livro
    unsigned capitulos; // Número total de capítulos do livro
    char titulo[100];   // Título do livro
    char autor[100];    // Nome do autor do livro
    int ano_publicacao; // Ano de publicação do livro
};

struct Jogo
{
    float vida;         // Pontos de vida do jogador (HP)
    float colete;       // Pontos de proteção do colete (Armor)
    unsigned inventario; // Quantidade de itens no inventário
    char nome[50];      // Nome do jogo
    char genero[50];    // Gênero do jogo (ex: Ação, RPG, Estratégia)
    float tempo_jogado; // Tempo total jogado em horas
};



int main()
{


   

	return 0;
}
